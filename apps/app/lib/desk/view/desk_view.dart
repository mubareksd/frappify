import 'package:flutter/material.dart';

class DeskView extends StatelessWidget {
  const DeskView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Desk')));
  }
}
